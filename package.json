{
  "name": "tidal-collaborative-playlists",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute tidal-collab-db --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute tidal-collab-db --remote --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250327.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.5.0"
  }
}
